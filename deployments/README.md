# Deployments (placeholders)

Container and infrastructure artifacts will live here when implementation begins.

| Path | Planned contents | Status |
|------|------------------|--------|
| `docker/` | Dockerfiles, Compose for local multi-service | Empty |
| `helm/` | Kubernetes Helm charts | Empty |
| `terraform/` | Cloud IaC (provider undecided) | Empty |

Do not add provider-specific resources or service images until those layers are intentionally implemented.
