# Shared infrastructure packages

Reserved for reusable **platform** concerns only (Kafka/Redis/Postgres helpers, logging, telemetry).

Do **not** put business/domain packages here.

No Go modules or implementations exist yet—packages will be introduced when the first consumer needs them.
